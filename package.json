{
  "name": "cribl-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "eslint-config-alloy": "^4.6.2",
    "sass": "^1.54.3",
    "vite": "^3.0.4",
    "vite-plugin-eslint": "^1.7.0"
  },
  "eslintConfig": {
    "extends": "alloy",
    "rules": {
      "max-params": [
        "off",
        true
      ],
      "no-unused-vars": [
        "warn",
        {
          "argsIgnorePattern": "^_",
          "varsIgnorePattern": "^_"
        }
      ]
    }
  }
}
